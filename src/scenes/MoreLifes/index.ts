import { BASE_URL, PATH_URL } from '@/const'
export default class MoreLifesScene extends Phaser.Scene {
  constructor() {
    super('MoreLifesScene')
  }

  preload() {
    console.log('morelifesscene preload...')
  }

  create() {}
}
